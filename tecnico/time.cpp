#include <bits/stdc++.h>
using namespace std;

#define fr(i,n) for(int i=0;i<n;i++)

clock_t ts; // time where the clock starts
double get_time(){
	return (double)(clock() - ts)/CLOCKS_PER_SEC;
}

int main(void) {
    ts = clock();
    
    /* Do your stuff here */
    fr(i,1e8) continue;
   
    printf("Time taken: %.2lfs\n", get_time());
    return 0;
}
